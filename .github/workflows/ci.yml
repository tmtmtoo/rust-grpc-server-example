name: CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: icepuma/rust-action@master
      with:
          args: cargo fmt -- --check && cargo clippy -- -D warnings && cargo test
